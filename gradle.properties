org.gradle.jvmargs=-Xmx2560m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# CloudConvert API key for the PDF -> Word/PowerPoint/Excel conversion feature.
# Get a free key at https://cloudconvert.com/dashboard/api/v2/keys and put it here
# (never commit a real key to source control - keep this file out of git if you set one).
CLOUDCONVERT_API_KEY=
